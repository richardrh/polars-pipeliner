repos:
  - repo: local
    hooks:
      - id: ruff-format
        name: Ruff format
        entry: uv run --frozen ruff format
        language: unsupported
        types_or: [python, pyi, markdown]
        pass_filenames: true
        require_serial: true
